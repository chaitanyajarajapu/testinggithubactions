name: myfirstworkflow
on:
  # push:
  #   branches:
  #     - main
  pull_request:
    branches:
      - main
  # workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: failurestep
        run: echo "Hello World chaitu"

  second_job:
    runs-on: windows-latest
    steps:  
      - name: step2
        shell: bash
        run: echo "Ths runner is $RUNNER_OS"

